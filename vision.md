# Vision

**User-stated:** Publish PAL publicly so other developers can use it.

**Observed:** PAL wraps persistent Codex, Pi, and Claude Code CLI sessions and exposes them through a local MCP server.

**Agent-selected working policy:** Keep installation small and dependency-free. PAL is an explicit delegation tool, not a sandbox, code-acceptance gate, or production orchestrator.
